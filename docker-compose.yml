version: '3.9'
services:
  api:
    build: .
    container_name: backendpath-api
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - ./:/app
    command: ["bun", "run", "src/index.ts"]
